var expect = require('chai').expect

describe('functionToTest()', function(){
	it('should do whatever it is supposed to', function(){
		var results = true // should be the output of the function we're testing
		expect(results).to.equal(true) // whatever the expected output is
	})
})
